## Check the project CLI

Use the project-local installed Wrangler and its --version/--help. Inspect package.json and the lockfile before selecting a command. Install only when required for authorized implementation; do not upgrade to latest as a prerequisite for reading configuration.
