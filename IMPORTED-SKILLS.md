# Imported skill sources

On 2026-09-04 these installed Codex and Claude skill trees were byte/mode compared and consolidated here: agterm, wrangler, agents-sdk, cloudflare, cloudflare-email-service, durable-objects, sandbox-sdk, turnstile-spin, web-perf, workers-best-practices. Both providers now link to this repository. This repository owns subsequent edits; do not overwrite these trees with a provider installation without reviewing the diff.

The installed copies did not supply a verified upstream revision. No upstream pin is claimed. The original installed file SHA-256 and mode inventory is in [imported-skills-manifest.json](imported-skills-manifest.json). That inventory records pre-consolidation inputs, not current-file checksums. Local originals are retained outside skill discovery at /mnt/agents/{codex,claude}/skill-backups-20260904-audit/.

Agterm and Wrangler entrypoints were split into task-specific guides. Cloudflare-family trigger descriptions were narrowed before import. Review future upstream updates against the recorded inputs, retaining these local routing and workflow changes.
